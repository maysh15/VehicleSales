import { VehicleMostSoldResult } from './vehicle-most-sold-result.model';

describe('VehicleMostSoldResult', () => {
  it('should create an instance', () => {
    expect(new VehicleMostSoldResult()).toBeTruthy();
  });
});
